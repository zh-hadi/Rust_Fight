fn main() {
    // ownership 
    // this mean momory free dynamicly 
    // Scope outer value delete dynamicly

    // Ownership rouls
    // -1. Each value in Rust has an owner.
    // -2. There can only be one owner at a time.
    // -3. When the owner goes out of scope, the value will be dropped.
}
